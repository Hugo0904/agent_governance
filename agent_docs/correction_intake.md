# 修正吸收流程

本文件用於處理「使用者 / reviewer 指正 AI」後的吸收與沉澱，不用於一般功能實作。

## 目的
- 讓 AI 被指正後，不只修正當前程式，還會判斷是否應沉澱成長期規則。
- 避免把單次失誤、未確認語意或臨時決策直接寫死成規範。
- 讓重複 review 意見逐步轉成 AI 可重用的專案記憶，降低後續 code review 成本。

## 分類（強制）
- `project_domain`：
  目標專案特有的業務語意、資料模型真相、功能規則。
- `project_architecture`：
  目標專案特有的框架版本、技術棧限制、架構邊界、目錄限制。
- `workspace_principle`：
  跨專案通用的工程觀念、分層原則、設計與重構方法。
- `workflow_governance`：
  AI 如何導讀、如何吸收指正、何時更新 md / test / checklist 的流程規則。
- `task_local`：
  只對當前任務成立的暫時決策或例外，不應升級成長期規則。
- `code_only`：
  單次實作疏失、 typo、漏判斷等，不構成長期規則。

## 判斷順序（強制）
1. 先分類，不可一被指正就直接新增 md。
2. 再判斷資訊是否足夠確認。
3. 最後才決定落點：改 code、記 task、更新專案規則、或更新 hub 規則。

## 確認等級（強制）
- `confirmed`：
  已有足夠上下文、程式依據或使用者明確確認，可寫成規則。
- `partial`：
  有方向但細節未完整，僅可先記待確認，不可寫成硬規則。
- `unconfirmed`：
  只是示例、猜測或片段描述，不可沉澱成長期規則。

## 落點規則（強制）
- `project_domain`、`project_architecture`：
  更新目標專案自己的 `AGENTS.md` 或專案規則檔。
- `workspace_principle`、`workflow_governance`：
  更新 `<WORKSPACE_ROOT>/agent_governance/agent_docs/` 對應文件。
- `task_local`：
  只記錄在 task / progress，不更新長期 md。
- `code_only`：
  只修 code，不更新長期 md。

## 升級規則（強制）
- 第一次發生：
  先修正，判斷是否具有重複風險。
- 同類錯誤第二次發生：
  至少補 md，避免 reviewer 重複口頭提醒。
- 若錯誤高風險、容易回歸、或僅靠文字規則不足：
  md 之外再補 test、checklist、grep 檢查或其他可執行控制。

## 何時改 `AGENTS.md`
- 問題出在「AI 沒先讀到正確上下文」時，應調整 `AGENTS.md` 的條件導讀。
- 問題出在「已讀到上下文但內容仍不足」時，應更新對應主題文件，而不是把細節硬塞進 `AGENTS.md`。

## 不可直接寫成規則的情況
- 使用者只是舉例，未確認真實業務語意。
- 尚未查清專案實作、資料來源或責任邊界。
- 只屬於當次需求的特殊處理。
- 文件已經有規則，這次只是沒有遵守；此時應優先修正流程或驗證機制。

## 文案原則
- 規則必須可觀察、可驗證，避免只寫抽象口號。
- 優先寫成「什麼情況、不能做什麼、應改去哪一層」。
- 不確定時先標記待確認，不要把猜測寫成真相。
